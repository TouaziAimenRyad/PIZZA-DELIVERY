<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/jquery-3.6.0.js"></script>
    <script src="/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/bootstrap.min.css" />
    <link href="/styles/styles_liv.css" rel="stylesheet" />
    <title>Livreur</title>
</head>
<body>
    <h3>votre livraison :</h3>
    <ul>
        <li><p>Client: <%= commande.nom+" "+commande.prenom %></p></li>
        <li><p>Adresse: <%= commande.adresse %></p></li>
        <li>
            <p>Panier:</p>
            <ul>
                <% commande.products.forEach(x=>{ %>
                    <% if (x.type!="menu") { %>
                        <li><p>produit:<%= x.nom %> quantité: <%= x.qty %></p></li>
                    <% }else{ %>
                
                        <li><p>produit:<%= x.type %> <%= x.type %> </p></li>
                    <% } %>
                <% }) %>
            </ul>
        </li>
    </ul>
    <a href="/livreur">retour</a>
</body>
</html>